$('#flash').html("<%= escape_javascript raw(flash_display) %>");
<% if @review.valid? %>
	$('.review_pagination_controller')
		.append("<%= escape_javascript(render :partial => 'dishes/review',
						:locals => { :review => @review }) %>");
	$('#review_description').val('');
<% end %>
